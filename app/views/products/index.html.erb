<h1 class="text-2xl font-bold mb-4">Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product, class: 'text-blue-500 hover:text-blue-700' %>
    </div>
  <% end %>
</div>
